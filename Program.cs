using System;

namespace HomeworkEquivalence
{
    class Program
    {
        //Оператор == для типа данных string сравнивает не ссылки а содержимое, и применимо к string оператор == не является виртуальным.
        //Для того чтобы при сравнении через оператор == вызвался переопределенный оператор Equals, необходимо переопределить оператор ==.

    }
}
